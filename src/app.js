const messages=document.getElementById('messages'),promptBox=document.getElementById('prompt'),sendButton=document.getElementById('sendButton'),newChat=document.getElementById('newChat');
function addMessage(text,role){const w=document.createElement('div');w.className=`message ${role}`;const b=document.createElement('div');b.className='message-bubble';b.textContent=text;w.appendChild(b);messages.appendChild(w);messages.scrollTop=messages.scrollHeight}
function bindSuggestions(){document.querySelectorAll('[data-prompt]').forEach(b=>b.addEventListener('click',()=>{promptBox.value=b.dataset.prompt;promptBox.focus()}))}
function sendMessage(){const text=promptBox.value.trim();if(!text)return;const welcome=document.querySelector('.welcome');if(welcome)welcome.remove();addMessage(text,'user');promptBox.value='';promptBox.style.height='auto';sendButton.disabled=true;setTimeout(()=>{addMessage('Frontend demo mode is active. The Qwen3-Coder backend is not connected yet. This interface is ready for the next stage, where YourAI will send requests to the model, inspect project files, run tools, test code, and fix errors.','ai');sendButton.disabled=false},450)}
sendButton.addEventListener('click',sendMessage);promptBox.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendMessage()}});promptBox.addEventListener('input',()=>{promptBox.style.height='auto';promptBox.style.height=Math.min(promptBox.scrollHeight,180)+'px'});bindSuggestions();newChat.addEventListener('click',()=>{messages.innerHTML='<div class="welcome"><div class="welcome-logo">Y</div><h1>Build with YourAI</h1><p>A free programming AI interface, ready for the Qwen3-Coder backend.</p><div class="suggestions"><button data-prompt="Explain how this project should be structured.">Explain my project</button><button data-prompt="Find potential bugs in my code.">Find bugs</button><button data-prompt="Build a clean feature from my requirements.">Build a feature</button></div></div>';bindSuggestions()});document.querySelectorAll('.side-item').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.side-item').forEach(b=>b.classList.remove('active'));button.classList.add('active');document.querySelectorAll('.panel').forEach(p=>p.classList.add('hidden'));document.getElementById(button.dataset.panel+'Panel').classList.remove('hidden')}));
